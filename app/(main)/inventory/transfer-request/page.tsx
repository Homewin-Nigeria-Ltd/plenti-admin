export const metadata = {
  title: "Transfer Request | Inventory | Plenti Admin",
};

import { TransferRequestView } from "@/components/inventory/TransferRequestView";

export default function TransferRequestPage() {
  return <TransferRequestView />;
}
