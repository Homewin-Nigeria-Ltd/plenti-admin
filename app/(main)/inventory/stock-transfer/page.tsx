export const metadata = {
  title: "Stock Transfer | Inventory | Plenti Admin",
};

import { StockTransferView } from "@/components/inventory/StockTransferView";

export default function StockTransferPage() {
  return <StockTransferView />;
}
