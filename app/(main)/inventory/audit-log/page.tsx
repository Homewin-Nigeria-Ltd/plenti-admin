export const metadata = {
  title: "Audit Log | Inventory | Plenti Admin",
};

import { AuditLogView } from "@/components/inventory/AuditLogView";

export default function AuditLogPage() {
  return <AuditLogView />;
}
